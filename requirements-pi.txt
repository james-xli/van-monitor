# Pip-only dependencies (installed into .venv on the Pi).
#
# Hardware/display libs (Pillow, spidev, gpiozero) come from apt via setup_pi.sh.

bleak>=0.21.0
bleak-retry-connector>=3.0.0
litime-ble>=0.2.0
victron-ble>=0.9.0
SolixBLE>=3.7.0
cryptography>=42.0.0
pycryptodome>=3.20.0
